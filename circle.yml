dependencies:
  pre:
    - pyenv local 3.6
    - sudo apt-get install tk-dev python-tk
    - pip install -r requirements.txt
    - python setup.py develop
test:
  override:
    - nosetests:
        timeout: 1800
