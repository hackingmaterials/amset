dependencies:
  pre:
    - sudo apt-get install python3-tk
    - sudo apt-get install python27-tkinter
  override:
    - pyenv local 2.7.12 3.4.4
    - pip install -r requirements.txt
    - python setup.py develop
test:
  override:
    - nosetests